# XCLI - XMBL Command Line

XMBL's Command Line module (bash).


This is a low-level command-line application that manages the xmbl system. It has all functionality of the other modules, as well as export and streaming features to monitor the system.

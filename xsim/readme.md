# XSIM - XMBL Simulator

XMBL's Simulator module.

This simulator runs infinitely. It has random qualities for every type of interaction and activity in the total xmbl system, including creation of identities, posting txs, validations, storage, compute, state machine diff txs, assembling app-centric diffs into current state
